[package]
name = "mini-a"
version = "0.0.0"
edition = "2021"
publish = false

[package.metadata.spel]
extension_attr = "mini_a"

[[package.metadata.spel.inject]]
wrapper = "require_a"

  [[package.metadata.spel.inject.account]]
  name = "a_config"
  seed = [{ const = "a_config" }]

[package.metadata.spel.embedded]
state_type = "mini_a::AConfig"

[dependencies]
mini-macros = { path = "../mini_macros" }
spel-framework = { path = "../../../../spel-framework" }
